name: lint_arch
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  analyzer: ^7.6.0
  analyzer_plugin: ^0.13.4
  custom_lint_builder: ^0.8.0

dev_dependencies:
  flutter_lints: ^6.0.0
